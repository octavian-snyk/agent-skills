# cli-parallel-tests

Broad automated validation overlay for the **CLI product** repository. Drives `package.json` / Turbo scripts rather than hardcoding tooling. Pairs with `cli-technical-analysis` after large runs.

See `SKILL.md`.
