# cli-contributor

Overlay for contributors working in the **CLI product** repository. Pairs with `tdd` and `repository-technical-analysis` for package-manager usage, `package.json` scripts, MR practices, and monorepo (pnpm / Turbo) expectations. Agent- and IDE-agnostic.

See `SKILL.md`.
