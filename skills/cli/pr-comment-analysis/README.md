# cli-pr-comment-analysis

GitLab MR grouped-comment workflow overlay for the **CLI product** repository: consumes `gitlab-mr-comment-analysis` outputs and enriches them with repo-specific analysis and proposed fixes.

See `SKILL.md`.
