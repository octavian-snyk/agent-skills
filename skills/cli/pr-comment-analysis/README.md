# cli-pr-comment-analysis

GitHub PR grouped-comment workflow overlay for the **CLI product** repository: deepens each `### issue_*` subsection inside **`review_pr_<PR>.md`** or **`analysis_pr_<PR>.md`** after **`github-pr-comment-analysis`** has grouped threads there. Transport uses **`github`**.

See `SKILL.md`.
