# cli-pr-comment-analysis

GitHub PR grouped-comment workflow overlay for the **CLI product** repository: consumes **`github-pr-comment-analysis`** outputs (`work_plan_pr_<PR>.md` and related files) and enriches them with repo-specific analysis and proposed fixes. Transport uses **`github`**.

See `SKILL.md`.
