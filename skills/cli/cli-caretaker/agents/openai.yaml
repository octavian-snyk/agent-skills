interface:
  display_name: "CLI Ask Caretaker"
  short_description: "Research CLI asks and advise actions"
  default_prompt: "Use $cli-caretaker to research the CLI Ask queue and @ask-cli-caretaker Slack questions, include relevant Datadog evidence, and write a read-only report with advised actions; exclude On Caller duties."
