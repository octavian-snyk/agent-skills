interface:
  display_name: "CLI Branch Change Reviewer"
  short_description: "Review CLI branches for bugs and excess complexity"
  default_prompt: "Use $cli-branch-change-reviewer to review the current CLI branch against origin/main with CLI-specific correctness and Ponytail simplification findings."
